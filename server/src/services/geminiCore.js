/**
 * @deprecated Import from geminiWrapper.js — kept for backward compatibility.
 */
export {
  createGeminiAgent,
  getMaxMessageLength,
  trimHistory,
  pingGemini,
  probeAllAgents,
} from './geminiWrapper.js';
